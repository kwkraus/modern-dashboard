import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap", // Better font loading performance
  preload: true,
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

export const metadata: Metadata = {
  title: {
    default: "Modern Dashboard",
    template: "%s | Modern Dashboard",
  },
  description: "A beautiful and responsive dashboard built with Next.js, featuring real-time analytics and modern UI components.",
  keywords: ["dashboard", "analytics", "next.js", "react", "typescript", "tailwind"],
  authors: [{ name: "Dashboard Team" }],
  creator: "Dashboard Team",
  metadataBase: new URL(process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000'),
  openGraph: {
    type: "website",
    locale: "en_US",
    url: "./",
    title: "Modern Dashboard",
    description: "A beautiful and responsive dashboard built with Next.js",
    siteName: "Modern Dashboard",
  },
  twitter: {
    card: "summary_large_image",
    title: "Modern Dashboard",
    description: "A beautiful and responsive dashboard built with Next.js",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      'max-video-preview': -1,
      'max-image-preview': 'large',
      'max-snippet': -1,
    },
  },
  verification: {
    // Add verification tokens when ready for production
    // google: "verification-token",
    // yandex: "verification-token",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
